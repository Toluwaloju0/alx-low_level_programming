#include "main.h"
/**
* swap_int - to swap two integers
* int a - an integer
* int b - an integer
* @a: an integer
* @b: an integer
*/
void swap_int(int *a, int *b)
{
	int *one = a;
	int *two = b;

	*one = 'b';
	*two = 'a';
}
