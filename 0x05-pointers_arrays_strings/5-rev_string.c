#include "main.h"
#include <string.h>
/**
* rev_string - to reverse a string
* @s: a string to be reversed
* Return: void
*/
void rev_string(char *s)
{
	int i = strlen(s) - 1

	while (i >= 0)
	{
		i--;
	}
}
