#include "main.h"

/**
 * is_prime - To check if a number is a prime number
 * @n: the number to be checked
 * Return: 1 if number is prime else 0
 */

int is_prime_number(int n);

{

	if (n == 0)
	{
		return (0);
	}
	if (n == 1)
	{
		return (1);
	}
	is_prime_number(n % (n % 3));
}

